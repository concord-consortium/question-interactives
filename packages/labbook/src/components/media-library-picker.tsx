import React from "react";

interface IProps {}

export const MediaLibraryPicker: React.FC<IProps> = () => {
  return (
    <div>
      Pick an item from the Activity Media Library.
    </div>
  );
};
